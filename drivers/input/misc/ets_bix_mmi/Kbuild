ccflags-$(CONFIG_EGIS_PANEL_NOTIFICATIONS) += -DCONFIG_PANEL_NOTIFICATIONS
ccflags-$(CONFIG_EGIS_DRM_PANEL_NOTIFICATIONS) += -DCONFIG_DRM_PANEL_NOTIFICATIONS

ets_bix_mmi-y += etxxx_fp.o

obj-$(CONFIG_INPUT_ETS_BIX_MMI) += ets_bix_mmi.o
